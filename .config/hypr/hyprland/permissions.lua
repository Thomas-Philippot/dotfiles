-- https://wiki.hypr.land/Configuring/Permissions/
-- Shared defaults — do not edit. No custom/permissions.lua override exists; add one if needed.
-- Permission changes require a Hyprland restart, not a reload.

-- hl.config({ ecosystem = { enforce_permissions = 1 } })

-- hl.permission({ path = "/usr/(bin|local/bin)/grim", capability = "screencopy", action = "allow" })
-- hl.permission({ path = "/usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland", capability = "screencopy", action = "allow" })
-- hl.permission({ path = "/usr/(bin|local/bin)/hyprpm", capability = "plugin", action = "allow" })
